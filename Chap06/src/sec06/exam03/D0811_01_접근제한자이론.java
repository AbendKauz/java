package sec06.exam03;

public class D0811_01_접근제한자이론 {

    // 접근제한자 -> 접근을 제한하기 위해 사용
    // : 클래스, 필드, 메소드, 생성자 선언 부분 앞에 작성

    // 3가지 / default

    // 1. public : 외부 클래스 모두 자유롭게 사용 가능
    // 2. protected : 같은 패키지 또는 자식 클래스에서 사용 가능
    // 3. private : 외부에서 사용 불가

    // 4. default : 같은 패키지에 소속된 클래스에서만 사용 가능

    // * (접근제한 약) public -> protected -> default -> private (접근제한 강)

}
