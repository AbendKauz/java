	- private로 막는 이유
		- 외부에서 마음대로 변경할 경우 객체의 무결성(결점이 없는 성질)이 깨질 수 있음

- Getter 메소드
	- 외부로 필드값을 전달
	-- 필드값을 가공해서 외부로 전달할 수 있음
- Setter 메소드
	- 외부의 값을 받아 필드의 값을 변경
	-- 매개값 검증하여 유효한 값만 필드로 저장할 수 있음
	
	- Getter, Setter 메소드 사용 시
	// public 리턴타입 get필드명(){		// 필드명의 첫글자는 대문자로
	// 		return 필드명;
	// }
	// public void set필드명(매개변수타입 변수명){	// 필드명의 첫글자는 대문자로
	// 		this.필드명 = 필드명;
	// }
	
	